spring:
  datasource:
    url: jdbc:h2:mem:testdb_test
  jpa:
    show-sql: false
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

logging:
  level:
    dev.com.andrsn: INFO
